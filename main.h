#ifndef MAIN_H /*guard*/
#define MAIN_H
#define give_us return /*using a macro to change the  return function to the name give_us*/

int _printf(const char *format, ...);
int putchar(int c);

#endif /*MAIN_H*/
